<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MaterialMenu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="textInputStyle">@style/Widget.App.TextInputLayout</item>
    </style>

    <style name="Widget.App.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="materialThemeOverlay">@style/Custom.TextInputLayout</item>
        <item name="shapeAppearance">@style/ShapeAppearance.Material3.Corner.Full</item>
        <!--<item name="hintTextColor">?android:textColorPrimary</item>-->
    </style>

    <style name="Custom.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="android:padding">0dp</item>
        <item name="materialThemeOverlay">@style/Custom.AutoCompleteTextView</item>
        <item name="android:layout_marginEnd">0dp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="Custom.AutoCompleteTextView" parent="ThemeOverlay.Material3.AutoCompleteTextView.OutlinedBox">
        <item name="android:textAlignment">textEnd</item>
    </style>

    <style name="Theme.MaterialMenu" parent="Base.Theme.MaterialMenu" />
</resources>